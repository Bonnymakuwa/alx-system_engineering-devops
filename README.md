# alx-system_engineering-devops
# fatal: No configured push destination
#                       # Remote                             #local:#remote branch
git push --set-upstream https://github.com/Bonnymakuwa/repo.git master:master 

# use `--force` flag on conflict
